import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Pode ajustar configurações conforme necessário (images, headers, etc.)
}

export default nextConfig
